info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/product
  body:
    type: json
    data: |-
      {
        "name": "Pizza",
        "price": 100
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
